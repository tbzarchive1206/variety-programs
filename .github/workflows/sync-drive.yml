name: Sync Variety and Programs

on:
  schedule:
    - cron: "17 5,17 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.16.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Read Google Drive archive
        run: pnpm run sync:drive
        env:
          GOOGLE_DRIVE_API_KEY: ${{ secrets.GOOGLE_DRIVE_API_KEY }}
      - run: pnpm test
      - name: Commit changed archive index
        run: |
          if git diff --quiet -- app/data/archive.generated.json; then exit 0; fi
          git config user.name "variety-programs-bot"
          git config user.email "variety-programs-bot@users.noreply.github.com"
          git add app/data/archive.generated.json
          git commit -m "chore: sync Variety and Programs archive"
          git push
